[project]
name = "orbitx"
version = "0.1.0"
description = "OrbitX marketing data integration platform"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "pydantic>=2.12.5",
    "common",
    "engine",
    "server",
    "dagster-orbitx",
    "pytest>=9.0.2",
]

[tool.uv.sources]
common = { workspace = true }
engine = { workspace = true }
server = { workspace = true }
dagster-orbitx = { workspace = true }

[tool.uv.workspace]
members = ["common", "server", "engine", "dagster"]

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]
ignore = ["E713", "E221", "B008"]

[tool.ruff.lint.isort]
known-first-party = ["common", "server", "engine", "dagster_orbitx"]

[tool.mypy]
python_version = "3.13"
plugins = ["pydantic.mypy"]
ignore_missing_imports = true
warn_return_any = true
strict_optional = true
show_error_codes = true

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
